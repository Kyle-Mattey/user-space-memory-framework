

# i386 OS

## running user-space memory allocator

To run the userspaec memory allocatre run the following commands:

```
user@system ~ $ gcc src/allocator.c src/allocatortest.c -o memtest

user@system ~ $ ./memtest
```
